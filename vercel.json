{
  "version": 2,
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && next build",
  "framework": "nextjs",
  "env": {
    "DATABASE_URL": "@india-tax-database-url",
    "DIRECT_URL": "@india-tax-direct-url"
  }
}
